let user = {     // an object
    name: "John",  // by key "name" store value "John"
    age: 30        // by key "age" store value 30
  };

alert(user.name);
alert(user.age)

user.isAdmin = true;
alert(isAdmin);

delete user.age;